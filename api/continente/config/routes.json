{
  "routes": [
    {
      "method": "GET",
      "path": "/continentes",
      "handler": "continente.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/continentes/count",
      "handler": "continente.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/continentes/:id",
      "handler": "continente.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/continentes",
      "handler": "continente.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/continentes/:id",
      "handler": "continente.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/continentes/:id",
      "handler": "continente.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
